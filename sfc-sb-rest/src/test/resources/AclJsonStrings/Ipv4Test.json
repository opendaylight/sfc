{
  "acl": [
    {
      "acl-name": "ACL_NAME",
      "access-list-entries": {
        "ace": [
          {
            "rule-name": "RULE_NAME",
            "matches": {
              "destination-ipv4-network": "DESTINATION_IPV4_NETWORK",
              "source-ipv4-network": "SOURCE_IPV4_NETWORK",
              "source-port-range": {
                "upper-port": UPPER_PORT,
                "lower-port": LOWER_PORT
              },
              "destination-port-range": {
                "upper-port": UPPER_PORT,
                "lower-port": LOWER_PORT
              },
              "protocol": PROTOCOL,
              "dscp": DSCP
            },
            "actions": {
              "service-function-acl:rendered-service-path": "SERVICE_FUNCTION_ACL_RENDERED_SERVICE_PATH"
            }
          }
        ]
      }
    }
  ]
}
